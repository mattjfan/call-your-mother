<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/materialBlue</item>
        <item name="colorPrimaryDark">@color/materialDarkBlue</item>
        <item name="colorAccent">@color/materialAccentBlue</item>
        <item name="android:windowBackground">@color/materialWhite</item>
    </style>

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@color/materialWhite</item>
    </style>

    <style name="headerFontStyle">
        <item name="android:fontFamily">@font/roboto_light</item>
        <item name="android:textSize">48sp</item>
        <item name="android:textColor">@color/materialBlack</item>
        <item name="android:letterSpacing">0.0</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="subtitleFontStyle">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textColor">@color/materialBlack</item>
        <item name="android:textAlignment">center</item>
    </style>

    <style name="buttonStyle">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/materialWhite</item>
        <item name="android:buttonCornerRadius">400dp</item>
    </style>

    <style name="fabStyle">
        <item name="android:background">@color/materialDarkBlue</item>
    </style>

</resources>
